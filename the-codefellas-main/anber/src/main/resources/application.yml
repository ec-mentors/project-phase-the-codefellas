spring:
  datasource:
    url: jdbc:mysql://localhost/anber?useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

data:
  boolean:
    true: true
    false: false
  user-profile:
    profile-options:
      - email
      - password
      - name
      - country
      - homes
      - notificationsEnabled

